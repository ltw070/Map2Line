# [PRD] 프로젝트명: Map2Line (도면 기반 라인 자동 식별 시스템)

## 1. 프로젝트 개요 (Project Overview)

반도체 공장 내 정형화된 건물 레이아웃(푸른 선)과 기둥 좌표(붉은 표식)를 분석하여, 사용자가 입력한 지도 이미지(캡처, 부분 조각, 축소/확대본)가 어떤 라인의 어느 구역인지 실시간으로 판별하는 지능형 매칭 시스템입니다.

---

## 2. 제품의 목표 (Product Goals)

| 목표 | 설명 |
|------|------|
| 높은 식별력 | 비슷비슷한 라인 구조 속에서도 붉은색 앵커(기둥) 패턴을 활용해 99% 이상의 식별 정확도 달성 |
| 스케일 불변성 | 도면의 축소, 확대, 일부 캡처 이미지에서도 강건한(Robust) 매칭 성능 유지 |
| 자동화된 가이드 | 현장 작업자가 도면을 찾는 시간을 단축하고 정확한 위치 기반 정보를 제공 |

---

## 3. 핵심 기능 (Key Features)

### 3.1 하이브리드 식별 엔진 (Hybrid Recognition Engine)

- **CNN 기반 거친 분류 (Coarse Matching):** 딥러닝(CNN)을 활용해 입력 이미지의 전체적인 특징을 파악, 후보 라인군을 신속히 압축.
- **표식 기반 정밀 매칭 (Fine Matching):** 붉은색 기둥(Column)의 배치 기하학(Geometry)을 분석하여 최종 라인명과 좌표 확정.
- **Scale-Invariant 분석:** 이미지 크기 변화에 영향을 받지 않는 특징점(Keypoint) 추출 및 대조.

### 3.2 이미지 전처리 및 추출

- **색상 분리 (Color Segmentation):** 흰 바탕에서 푸른 선(구조)과 붉은 선/글자(표식)를 분리하여 독립적인 레이어로 처리.
- **앵커 포인트 탐지:** 글자 인식이 불가능할 정도의 저해상도에서도 붉은색 표식의 위치(Blob)를 찾아내어 별자리 매칭 수행.
- **OCR 검증:** 해상도가 확보된 경우 붉은색 기둥 번호를 텍스트로 읽어 최종 결과의 교차 검증 수행.

### 3.3 결과 시각화

- **라인명 및 좌표 리턴:** `"Line A - Section 102 (Column B4-B6)"` 형태의 결과 출력.
- **매칭 신뢰도 표시:** 분석 결과의 신뢰도(Confidence Score)를 % 로 시각화.

---

## 4. 기술 스택 (Technical Stack)

| 분류 | 기술 |
|------|------|
| 언어 | Python 3.9+ |
| 이미지 처리 | OpenCV (Color Space 변환, Contour 탐지, 특징점 매칭) |
| 딥러닝 | PyTorch / TensorFlow (CNN 모델링, SuperPoint/LoFTR 등 딥 매칭 활용) |
| OCR | EasyOCR 또는 PaddleOCR (붉은색 텍스트 추출용) |
| 백엔드 | FastAPI |
| 프론트엔드 | Streamlit 또는 React |

---

## 5. 데이터 전략 (Data Strategy)

- **학습 데이터 구성:** 원본 도면을 다양한 크기로 리사이징하고, 임의로 자른(Crop) 조각 이미지 생성.
- **데이터 증강 (Augmentation):** 노이즈 추가, 흐림(Blur) 처리, 일부 표식 가림 등을 통해 열악한 현장 이미지 상황 시뮬레이션 학습.

---

## 6. 성공 지표 (Success Metrics)

| 지표 | 목표값 |
|------|--------|
| 정확도 | 유사 레이아웃 라인 간 오분류율 1% 미만 |
| 반응 속도 | 이미지 업로드 후 식별 결과 출력까지 1.5초 이내 |
| 최소 요구사항 | 원본 대비 30% 이하로 축소된 이미지에서도 라인 식별 성공 |

---

## 7. 단계별 로드맵 (Roadmap)

| Phase | 내용 |
|-------|------|
| Phase 1 | OpenCV를 이용한 색상 분리 및 붉은 점(기둥) 배치 패턴 매칭 알고리즘 개발 |
| Phase 2 | CNN 모델을 추가하여 전체 라인 후보군을 1차 분류하는 하이브리드 엔진 구축 |
| Phase 3 | 축소/확대 대응 테스트 및 모바일 캡처 환경 최적화 |
